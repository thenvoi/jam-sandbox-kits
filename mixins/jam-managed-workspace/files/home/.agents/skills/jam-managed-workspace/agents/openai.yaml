interface:
  display_name: "Jam Managed Workspace"
  short_description: "Work safely across Jam sandbox repositories"
  default_prompt: "Use $jam-managed-workspace to inspect repositories, clone or branch as needed, commit staged work through the exact-session tool, and preserve work safely."
